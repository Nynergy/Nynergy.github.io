# Add robots.txt to generated _site/

- Created:  June 16, 2026
- Status:   OPEN

I copied an existing robots.txt file to this repo, but I must have forgotten to
add it to `totem` as part of the build process. We should check if the file
exists, and if it does, copy it to the top-level build directory. If it doesn't
exist, we can just ignore it.
